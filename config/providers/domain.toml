# Real (not example) provider config — read by xtask's Release Bundling
# Step (docs/proposal/16-agent-system-concern-split-and-release-bundling.md),
# never by the runtime MCP server. Registers Kriti's existing rust_dev
# Domain System content into the packaged release's data/mcp.db.
# Referenced from dharma-build.toml's [[release.providers]].

[domain_system]
name = "rust_dev"
version = "1.0.0"
description = "Rust/Cargo documentation domain system projected from base_dev (Kriti)"

[domain_system.content]
root_dir = "${DHARMA_DOMAIN_CONTENT_DIR:-/home/dell/PycharmProjects/Kriti/dharma/domain/system/dev/rust_dev}"
