# Real (not example) provider config — read by xtask's Release Bundling
# Step (docs/proposal/16-agent-system-concern-split-and-release-bundling.md),
# never by the runtime MCP server. Registers the `capability-provisioning`
# concern once Kriti splits its combined agent/system/analyse/ folder into
# per-concern subfolders (proposal 16, Constraints — precondition, not yet
# done: `root_dir` below does not exist on disk today, so this entry is
# skipped by the Release Bundling Step until it does).
# Referenced from dharma-build.toml's [[release.providers]].

[agent_system]
name = "capability-provisioning"
concern = "capability-provisioning"
description = "Finds every Agent/Skill able to execute a Domain System's Tasks; reports gaps"
is_privileged_request = false

[agent_system.content]
root_dir = "${DHARMA_AGENT_CAPABILITY_PROVISIONING_CONTENT_DIR:-/home/dell/PycharmProjects/Kriti/dharma/agent/system/capability-provisioning}"
